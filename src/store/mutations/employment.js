export const SET_COMPENSATION_TYPES = (state, payload) => {
  state.compensationTypes = payload;
};

export const SET_ADDITIONAL_EMPLOYMENT_INDEX = (state, payload) => {
  state.additionalEmploymentIndex = payload;
};
