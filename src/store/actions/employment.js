export const setCompensationTypes = ({ commit }, payload) => {
  commit("SET_COMPENSATION_TYPES", payload);
};

export const setAdditionalEmploymentIndex = ({ commit }, payload) => {
  commit("SET_ADDITIONAL_EMPLOYMENT_INDEX", payload);
};
